/* Reset básico */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  line-height: 1.6;
  background-color: #f9f9f9;
  color: #333;
}

header {
  background: linear-gradient(to right, #6fbf73, #3f9e4d);
  color: white;
  padding: 2rem;
  text-align: center;
}

header h1 {
  font-size: 2.5rem;
  margin-bottom: 0.5rem;
}

header p {
  font-size: 1.2rem;
}

.intro, .conexao {
  padding: 2rem;
  max-width: 800px;
  margin: auto;
  text-align: center;
}

.intro h2, .conexao h2 {
  font-size: 2rem;
  color: #3f9e4d;
  margin-bottom: 1rem;
}

.cards {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  padding: 2rem;
  gap: 2rem;
  background-color: #eef9f0;
}

.card {
  background-color: white;
  border-radius: 12px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
  overflow: hidden;
  max-width: 300px;
  text-align: center;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: scale(1.03);
}

.card img {
  width: 100%;
  height: 180px;
  object-fit: cover;
}

.card h3 {
  color: #2e7d32;
  margin: 1rem 0 0.5rem;
}

.card p {
  padding: 0 1rem 1rem;
}

footer {
  background-color: #3f9e4d;
  color: white;
  text-align: center;
  padding: 1rem;
}
